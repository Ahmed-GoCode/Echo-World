function love.conf(t)
    t.window.title = "Echo World"
    t.window.width = 0  -- 0 means use fullscreen width
    t.window.height = 0 -- 0 means use fullscreen height
    t.window.fullscreen = true  -- Enable fullscreen
    t.window.resizable = false  -- Usually you don't want resizable in fullscreen
end